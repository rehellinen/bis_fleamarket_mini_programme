.container{
  color: #6D6D6D;
  font-family: MicroSoft Yahei;
  font-size: 18px;
}

.disabled{
    pointer-events: none;
}

/* 加载中样式 */
.loading-container{
  width: 750rpx;
  min-height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
}

.loading-container image{
  width: 300rpx;
  height: 300rpx;
}